# Apple `container` environment (placeholder)

SoftGPU may use Apple's official [`container`](https://github.com/apple/container) project as an optional Linux ARM64 laboratory on Apple Silicon.

**Phase 0:** no recipes yet. Re-check macOS version requirements, CLI syntax, and networking from the upstream README before adding scripts.

This is **not** a SoftGPU runtime dependency and **not** the canonical ROCm integration gate (that remains Linux x86-64 + pinned ROCm).
