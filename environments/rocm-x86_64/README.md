# ROCm x86_64 environment (placeholder)

Canonical SoftGPU ↔ HIP/ROCr integration will use a **pinned official ROCm** container or image digest on Linux x86-64.

**Phase 0:** image digest and ROCm version pin are intentionally unset. Phase 1 must record the exact version, digest, and loader proof commands in `docs/sources.md` and this directory.
