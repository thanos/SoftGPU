# Conformance (Phase 0)

SoftGPU has **no** conformance claims in Phase 0.

The future ladder (layout → unit → property → fuzz → protocol → integration → golden artifacts → metamorphic → differential → hardware) is documented in the project charter. Integration and hardware levels must not be marked passed when skipped.

See `docs/support-matrix.md` and `docs/sources.md`.
